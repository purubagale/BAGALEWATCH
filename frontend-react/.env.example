# Left blank on purpose (2026-08-10 port-hiding pass) — see the real
# .env's own comment. Only set these if you need the built app to call a
# django/node-gateway instance that ISN'T reachable via the same origin.
VITE_DJANGO_API_URL=
VITE_NODE_GATEWAY_URL=
